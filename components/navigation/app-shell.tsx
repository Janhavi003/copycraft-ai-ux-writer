'use client';
import Link from 'next/link'; import {usePathname} from 'next/navigation'; import {useEffect,useState} from 'react'; import {BarChart3,BookOpen,FolderKanban,History,Menu,Moon,PenLine,Settings,Sun,Type,Volume2,X} from 'lucide-react';
const nav=[['/dashboard','Dashboard',BarChart3],['/studio','Copy Studio',PenLine],['/projects','Projects',FolderKanban],['/patterns','Pattern Library',BookOpen],['/history','History',History],['/brand-voice','Brand Voice',Volume2],['/settings','Settings',Settings]] as const;
export function AppShell({children}:{children:React.ReactNode}){const path=usePathname();const [open,setOpen]=useState(false);const [dark,setDark]=useState(false);useEffect(()=>{const v=localStorage.getItem('copycraft-theme');const d=v==='dark'||(!v&&window.matchMedia('(prefers-color-scheme: dark)').matches);setDark(d);document.documentElement.classList.toggle('dark',d)},[]);function toggle(){const d=!dark;setDark(d);document.documentElement.classList.toggle('dark',d);localStorage.setItem('copycraft-theme',d?'dark':'light')}if(path==='/')return <>{children}</>;return <div className="min-h-screen bg-[var(--bg)] text-[var(--text)]"><a href="#main-content" className="focus-ring sr-only focus:not-sr-only fixed left-3 top-3 z-[100] rounded-lg bg-[var(--surface)] px-4 py-3 text-sm font-bold shadow-lg">Skip to main content</a><aside className={`fixed inset-y-0 left-0 z-50 w-64 border-r border-line bg-[var(--surface)] p-4 transition-transform lg:translate-x-0 ${open?'translate-x-0':'-translate-x-full'}`}><div className="flex items-center justify-between px-2 py-2"><Link href="/dashboard" className="focus-ring flex items-center gap-2 font-bold" onClick={()=>setOpen(false)}><span className="grid h-8 w-8 place-items-center rounded-lg bg-[var(--accent)] text-white"><Type size={17}/></span>CopyCraft</Link><button className="focus-ring rounded-md p-2 lg:hidden" onClick={()=>setOpen(false)} aria-label="Close navigation"><X size={18}/></button></div><p className="px-2 pb-5 pt-1 text-xs muted">UX writing workspace</p><nav aria-label="Primary" className="space-y-1">{nav.map(([href,label,Icon])=>{const active=path===href||path.startsWith(href+'/');return <Link key={href} href={href} onClick={()=>setOpen(false)} className={`focus-ring flex min-h-11 items-center gap-3 rounded-lg px-3 text-sm font-medium ${active?'bg-[var(--accent)]/10 text-[var(--accent)]':'muted hover:bg-[var(--surface2)] hover:text-[var(--text)]'}`}><Icon size={18}/>{label}</Link>})}</nav><div className="mt-8 rounded-xl bg-[var(--surface2)] p-4"><div className="text-xs font-semibold">Demo mode</div><p className="mt-1 text-xs muted">Everything works locally without an API key.</p><Link className="focus-ring mt-3 inline-flex text-xs font-semibold accent" href="/settings">Configure API →</Link></div></aside><main id="main-content" tabIndex={-1} className="lg:pl-64"><header className="sticky top-0 z-40 flex min-h-16 items-center justify-between border-b border-line bg-[var(--bg)]/95 px-4 backdrop-blur md:px-8"><button className="focus-ring rounded-lg p-2 lg:hidden" onClick={()=>setOpen(true)} aria-label="Open navigation"><Menu/></button><div className="hidden text-xs muted lg:block">Workspace / {path.split('/')[1]||'dashboard'}</div><div className="ml-auto flex items-center gap-2"><button className="focus-ring grid h-10 w-10 place-items-center rounded-lg border border-line bg-[var(--surface)]" onClick={toggle} aria-label={dark?'Switch to light mode':'Switch to dark mode'}>{dark?<Sun size={17}/>:<Moon size={17}/>}</button><div className="hidden text-right sm:block"><div className="text-xs font-semibold">Local workspace</div><div className="text-[11px] muted">No account required</div></div></div></header><div className="mx-auto max-w-[1500px] px-4 py-6 md:px-8 md:py-8">{children}</div></main>{open?<button className="fixed inset-0 z-40 bg-black/30 lg:hidden" onClick={()=>setOpen(false)} aria-label="Close navigation overlay"/>:null}</div>}
