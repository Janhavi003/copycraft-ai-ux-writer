import './globals.css'; import {AppShell} from '@/components/navigation/app-shell'; export const metadata={title:'CopyCraft — UX writing workspace',description:'Generate, inspect and improve interface copy.'}; export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en" suppressHydrationWarning><body><AppShell>{children}</AppShell></body></html>}
